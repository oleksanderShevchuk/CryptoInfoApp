<Window x:Class="CryptoInfoApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="CryptoInfoApp" Height="450" Width="800">
    <Grid>
        <ListView ItemsSource="{Binding Coins}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Rank" DisplayMemberBinding="{Binding MarketCapRank}" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Symbol" DisplayMemberBinding="{Binding Symbol}" />
                    <GridViewColumn Header="Price" DisplayMemberBinding="{Binding CurrentPrice}" />
                    <GridViewColumn Header="Market Cap" DisplayMemberBinding="{Binding MarketCap}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>